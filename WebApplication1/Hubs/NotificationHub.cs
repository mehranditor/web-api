// Hubs/NotificationHub.cs
using Microsoft.AspNetCore.SignalR;

namespace WebApplication1.Hubs
{
    public class NotificationHub : Hub
    {
        // This hub can be empty for now—it's just for broadcasting notifications.
        // You can add methods here later if clients need to invoke server-side logic (e.g., JoinGroup).
    }
}